# Environment — never commit secrets
.env

# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.pytest_cache/

# Runs (large JSON files — commit selectively)
runs/*.json
!runs/.gitkeep

# TTS audio cache — regenerated on demand from text
runs/tts_cache/

# macOS
.DS_Store
